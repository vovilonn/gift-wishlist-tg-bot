{
  "name": "tg-gift-wishlist",
  "private": true,
  "workspaces": [
    "backend",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace backend && npm run build --workspace web",
    "lint": "npm run lint --workspace backend && npm run lint --workspace web",
    "typecheck": "npm run typecheck --workspace backend && npm run typecheck --workspace web",
    "test": "npm run test --workspace backend"
  }
}
